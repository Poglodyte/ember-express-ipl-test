import Route from '@ember/routing/route';

export default Route.extend({
  model() {
  //  return this.store.findAll('manhole');
    // Trying to display query results by first loading all manholes here, then updating model in controller action
  }
});
